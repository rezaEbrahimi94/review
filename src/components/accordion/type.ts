export type TSamAccordionProps = {
  id: string,
  expandIcon?: React.ReactNode,
  summary: string,
  detailHeading: string,
  detailContent: string,
  detailButtonLabel?: string,
  detailButtonIcon?: React.ReactNode,
  detailHref?: string,
  disabled?: boolean,
}
